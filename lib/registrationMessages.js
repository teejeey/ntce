/** Registration API + service copy for missing required fields (single source of truth). */
export const REGISTRATION_REQUIRED_FIELDS_MESSAGE =
  "Full name, email, mobile number, organization, designation, and topic interest are required.";
